# =============================================================================
# Docker — Variables for docker-compose (MariaDB, etc.)
# =============================================================================
DB_ROOT_PASSWORD=root
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress

# =============================================================================
# Remote Sync — Only needed if using remote-sync or both dev modes
# =============================================================================
# REMOTE_USER=usuario
# REMOTE_HOST=servidor.com
# REMOTE_PORT=22
# REMOTE_THEME_PATH=/var/www/html/wp-content/themes/__THEME_SLUG__
# TUNNEL_TOOL=cloudflared
# TUNNEL_DOMAIN=mi-sitio.com
# SYNC_EXCLUDE=.git,node_modules,.DS_Store,*.log,.env,public/hot
# SYNC_DELETE=false
