{
  "name": "__PROJECT_SLUG__",
  "private": true,
  "description": "WordPress Bedrock + Vite boilerplate",
  "scripts": {
    "setup": "node setup.js",
    "sync": "bash scripts/sync-watch.sh",
    "sync:push": "bash scripts/sync-push.sh",
    "sync:pull": "bash scripts/sync-pull.sh",
    "tunnel": "bash scripts/tunnel.sh",
    "sync:tunnel": "concurrently \"npm run sync\" \"npm run tunnel\"",
    "setup:remote": "bash scripts/setup-remote.sh"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
