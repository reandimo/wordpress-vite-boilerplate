/**
 * __THEME_NAME__ – frontend entry point.
 * Import styles and initialize page components.
 */
import '../../styles/frontend/main.scss';

// Import your components here:
// import { ExampleCta } from '../components/example-cta';

document.addEventListener('DOMContentLoaded', () => {
    // Initialize your components here:
    // ExampleCta.initializeAll();
});
